# Everything in requirements.txt, plus what training, exporting and the
# experiments need.  The till itself does not import any of this.
-r requirements.txt
torch>=2.0
torchvision>=0.15
ultralytics>=8.0
onnx>=1.15
onnxscript>=0.1
scikit-learn>=1.3
matplotlib>=3.7
pytest>=8.0
httpx
# research-only encoders for the E3 ablation (MobileCLIP, SigLIP); MIT
open_clip_torch>=2.24
